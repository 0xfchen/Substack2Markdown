const ui = {
  backLink: '← Library',
  readingTime: (n: number) => `${n} min read`,
  updated: 'Updated',
  relatedPosts: 'Related',
  allPosts: 'Library →',
  readLink: 'Read →',
};

export function getUiText() {
  return ui;
}
